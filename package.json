{
  "name": "gkc-payment-system",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"pnpm --filter backend dev\" \"pnpm --filter frontend dev\"",
    "build": "pnpm --filter frontend build"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}